/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
  },
};

// biome-ignore lint/style/noDefaultExport: postcss require default export
export default config;
